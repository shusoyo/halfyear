#import "../setup/main.typ": *
#import "../setup/theorem.typ": *

#show: thmrules.with(qed-symbol: $square$)
#show: project.with(language: "ch")

= 一元函数微分学的应用（二）—— 中值定理、微分等式与微分不等式

== 中值定理

#theorem("有界与最值定理")[
  闭连开导，$m <= f(x) <= M ,space m,M$ 为区间最小值和最大值。
]

#theorem("介值定理")[
  闭连开导，当 $m<= mu <= M$ 时, 存在 $xi in [a,b], $ 使得$f(xi) = mu$.
]

#theorem("平均值定理")[
  + 闭连开导,当 $a < x_1 < x_2 < ···< x_n < b$ 时，在 $[x_1,x_n]$ 内至少存在一点 $xi$ 使得,
    $ f(xi) = (f(x_1) + f(x_2) + ··· + f(x_n)) / n. $
  + 连续型
    $ f(xi) = 1 / (b-a). integral^b_a f(x)d x $
]

#theorem("零点定理")[
  闭连开导，当 $f(a) ·f(b) < 0$ 时, 存在 $xi in (a,b),$ 使得 $f(xi) = 0$。
  #remark()[$f(a),f(b),a,b$ 为无穷大也行.同时也可以引申到导数零点定理.]
]

#theorem("fermat 定理")[
  在 $x_0$ 处可导且取极值，则 $f'(x) = 0$. //··············待补充证明
]

#theorem("Rolle 定理")[
  $f(x)$ 在区间[a,b]闭连开导,且 $f(a) = f(b)$ , 则存在 $f'(xi) = 0$.
  #remark("推广")[
    $f(x)$ 在 $(a,b)$ 可导, $lim_(x -> a^+)f(x) = lim_(x -> b^-)f(x) = A$ 则在 $(a,b)$ 内至少存在一点 $xi 使 f'(xi) = 0$.
    (其中a,b,A均可是无穷大).
  ]
  #remark("多个零点的罗尔定理")[
    $f(x)$ 每两个零点为一组,出现一个 $f'(alpha) = 0$ , 如果出现多个导数零点，则再次对 $f'(x)$ 进行罗尔中值定理。类似递归。
  ]
]

#remark("构造函数")[（不仅仅适用于罗尔定理）
  + 见到 $[f(x)f'(x)], F(x) = f^2(x)$.

  + 见到 $[f'(x)]^2 + f(x)f''(x), F(x) = f(x)f'(x)$.

  + 见到 $f'(x) + f(x)phi'(x), F(x) = f(x)e^(phi(x))$.

  + 见到 $ u''v + 2u'v' + 2u v'', (u v)''$.

  + 见到 $ f'(x)x - f(x), F(x) = f(x) / x$.

  + 见到 $f''(x)f(x) - [f'(x)]^2, F(x) = (f'(x)) / f(x) "或" F(x) = ln f(x)$.
]

#theorem("Lagrange 中值定理")[
  设f(x)满足闭连开导,则存在$xi in (a,b)$, 使得$f'(xi) = (f(b) - f(a))/(b - a)$
]

#theorem("Cauchy 中值定理")[
  设f(x),g(x)满足闭连开导，且$g'(x) != 0$, 则存在$xi in (a,b)$,使得

  $ (f(b) - f(a)) / (g(b) - g(a)) = (f'(xi)) / (g'(xi)) $
]

#theorem("Taylor 公式（ Lagrange 余项）")[
  设f(x)在 $x_0$ 的某个邻域内n+1阶导存在,则对该邻域内的任意点x,有
  $
    f(x) = f(x_0) + f'(x_0)(x - x_0) + …… + 1 / (n !) f^((n)) (x_0)(x - x_0)^n
    + (f^((n + 1)) (xi) ) / (n + 1)! (x - x_0)^(n + 1)
  $

  #remark[
    常用于区间[a,b]，在证明题中使用，证明不等式，中值等式.
  ]
]

#theorem("Taylor 公式（ Peano 余项）")[
  设 $f(x)$ 在 $x_0$ 处 $n$ 阶可导,则存在 $x_0$ 的一个邻域,对于该邻域内的任意点 $x$, 有
  $
    f(x) = f(x_0) + f'(x_0)(x - x_0) + …… + 1 / (n !) f^((n)) (x_0)(x - x_0)^n
    + o((x - x_0 )^n)
  $
  #remark[
    仅用于 $x_0 $ 与其邻域,常用于研究 $x = x_0$ 处的某些结论,如求极限,判断无穷小的阶数,判定极值等.
  ]

  #remark[
    $x_0$ 取 $0$ 时,为麦克劳林展开式.
  ]
]

#corollary("Maclaurin series")[常见的麦克劳林展开式，见公式章节]

== 微分等式
#theorem()[

  1. 零点定理(证明根的存在)\
    若 $f(x)$ 在[a,b]上连续,且f(a)f(b) < 0 ,则在(a,b)内至少存在一根\

  2. 单调性(证明根的唯一性)\
    若f(x)在(a,b)内单调,则f(x) = 0在区间内至多有一个根,可以是无穷区间\

  3. 罗尔定理及其推论\
    + $f(x)$ 在区间$I$上$n$阶可导,且 $f^((n)) (x) != 0$ , 即 $f^((n)) (x) = 0$ 无实根(至多 $0$ 个根) ,于是 $f(x) = 0$ 至多有 $n$ 个根
    + 实系数奇次方程至少有一个根
]



== 微分不等式

#theorem("证明不等式")[
  1. 使用单调性,凹凸性,最值

  2. 分离参数

  3. 中值定理,凑出f(x)或者g(x)
]
